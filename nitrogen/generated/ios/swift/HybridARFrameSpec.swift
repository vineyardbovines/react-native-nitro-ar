///
/// HybridARFrameSpec.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

import NitroModules

/// See ``HybridARFrameSpec``
public protocol HybridARFrameSpec_protocol: HybridObject {
  // Properties
  var timestamp: Double { get }
  var cameraPosition: [Double] { get }
  var cameraRotation: [Double] { get }
  var projectionMatrix: [Double] { get }
  var viewMatrix: [Double] { get }
  var cameraIntrinsics: [Double] { get }
  var imageResolution: [Double] { get }
  var lightEstimate: (any HybridARLightEstimateSpec)? { get }
  var directionalLightEstimate: (any HybridARDirectionalLightEstimateSpec)? { get }
  var sceneDepth: (any HybridARDepthDataSpec)? { get }
  var smoothedSceneDepth: (any HybridARDepthDataSpec)? { get }

  // Methods
  func getCapturedImage(quality: Double) throws -> String
}

public extension HybridARFrameSpec_protocol {
  /// Default implementation of ``HybridObject.toString``
  func toString() -> String {
    return "[HybridObject ARFrame]"
  }
}

/// See ``HybridARFrameSpec``
open class HybridARFrameSpec_base {
  private weak var cxxWrapper: HybridARFrameSpec_cxx? = nil
  public init() { }
  public func getCxxWrapper() -> HybridARFrameSpec_cxx {
  #if DEBUG
    guard self is any HybridARFrameSpec else {
      fatalError("`self` is not a `HybridARFrameSpec`! Did you accidentally inherit from `HybridARFrameSpec_base` instead of `HybridARFrameSpec`?")
    }
  #endif
    if let cxxWrapper = self.cxxWrapper {
      return cxxWrapper
    } else {
      let cxxWrapper = HybridARFrameSpec_cxx(self as! any HybridARFrameSpec)
      self.cxxWrapper = cxxWrapper
      return cxxWrapper
    }
  }
}

/**
 * A Swift base-protocol representing the ARFrame HybridObject.
 * Implement this protocol to create Swift-based instances of ARFrame.
 * ```swift
 * class HybridARFrame : HybridARFrameSpec {
 *   // ...
 * }
 * ```
 */
public typealias HybridARFrameSpec = HybridARFrameSpec_protocol & HybridARFrameSpec_base
